apiVersion: v1
kind: Service
metadata:
  name: app-green-service
spec:
  type: ClusterIP
  selector:
    app: python-basic-web
    version: green
  ports:
    - port: 80
      targetPort: http-port
      protocol: TCP
      name: http
